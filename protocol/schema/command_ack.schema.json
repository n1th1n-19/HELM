{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "command_ack.schema.json",
  "title": "CommandAck",
  "description": "Agent acknowledgement of a previously received command.",
  "type": "object",
  "required": ["id", "success"],
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "string",
      "format": "uuid",
      "description": "The command identifier from the originating command message"
    },
    "success": {
      "type": "boolean",
      "description": "true if the action completed successfully, false if it failed"
    },
    "message": {
      "type": ["string", "null"],
      "description": "Human-readable description of the outcome, error details, or additional context; null when not applicable"
    }
  }
}
