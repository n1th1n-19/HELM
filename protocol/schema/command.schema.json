{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "command.schema.json",
  "title": "Command",
  "description": "Command sent from the Android app to the desktop agent requesting an action.",
  "type": "object",
  "required": ["id", "action"],
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "string",
      "format": "uuid",
      "description": "Unique command identifier (UUID v4). The agent echoes this back in the corresponding command_ack."
    },
    "action": {
      "type": "string",
      "enum": [
        "restart_dev_server",
        "git_pull",
        "git_push",
        "open_terminal",
        "open_project",
        "lock",
        "suspend",
        "reboot",
        "shutdown"
      ],
      "description": "Action to execute on the workstation"
    },
    "args": {
      "type": "object",
      "description": "Action-specific parameters as freeform key-value pairs (e.g. {\"project_path\": \"/home/user/myapp\"})",
      "additionalProperties": {
        "type": "string"
      }
    }
  }
}
